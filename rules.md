follow the architecture.md file to build the project and all the rules set in rules.md file. 
build the project accroding to planeed implementation file in phase wise manner and after each phase ask for approval to build the next phase. 
remember to creat test for each phase and after each segment test it with the tests before going to next phases, only go to next phase if all tests are passed. 
maintain sepeate .env file for all api keys never push it to github
maintain a log file to log everything, 
maintain a separate artifacts file. 